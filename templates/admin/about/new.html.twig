{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter un contenu{% endblock %}

{% block body %}

    <section class="admininfos container edit">

        <a href="{{ path('adminabout') }}" class="adminback btn btn-primary"><i class="fas fa-arrow-left"></i> Retour aux sections "à propos"</a>

        <h2>Ajouter une section</h2>

        <form method="post" class="form" novalidate>
            <div class="form__group">
                           
                {{ form_label(form.name) }}
                {% if form.name.vars.errors|length %}
                        <div class="invalid">
                            { form_errors(form.name) }}
                        <i class="fas fa-times"></i>
                    </div>
                {% endif %}
                
                {{ form_widget(form.name) }}
            </div>

            <div class="form__group">
                           
                    {{ form_label(form.content) }}
                    {% if form.content.vars.errors|length %}
                            <div class="invalid">
                                { form_errors(form.content) }}
                            <i class="fas fa-times"></i>
                        </div>
                    {% endif %}
                    
                    {{ form_widget(form.content, { 'id': 'editor'}) }}
                </div>
        
            <div class="form__group">
                <input type="submit" value="Ajouter">
            </div>
    
            {{ form_rest(form) }}
    
        </form>

    </section>

{% endblock %}
